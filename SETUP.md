# Setup

1. Put these files in the `Sidhnath539/Sidhnath539` profile repository.
2. Keep `github-snake.yml` at `.github/workflows/github-snake.yml`.
3. Commit/push to `main`.
4. Enable GitHub Actions with read/write workflow permissions if the repository requires it.
5. Run the workflow once manually; it will then refresh the `output` branch daily.
